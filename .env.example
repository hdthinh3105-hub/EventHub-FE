# URL backend — là build-time, đổi phải build lại
# Local (npm + BE npm):        http://localhost:4000
# Docker (FE 8080 → BE 4000):  http://localhost:4000 (browser gọi host, không phải container)
# Production (Render):         https://eventhub-1lf8.onrender.com
VITE_API_URL=http://localhost:4000
